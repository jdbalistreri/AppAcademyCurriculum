<%= album_header(@album) %>

<%= render partial: "shared/index", locals: {objs: @tracks, name: "track"} %>

<br><br>
<em><%= @album.recording_type.capitalize %> album</em>

<br><br>

<a href="<%= new_album_track_url(@album) %>">Add a track!</a>

<br>
<a href="<%= edit_album_url %>">Edit album</a>
<br>

<form action="<%= album_url(@album) %>" method="post">
  <%= auth_token %>
  <input type="hidden" name="_method" value="DELETE">
  <button>Delete album</button>
</form>
