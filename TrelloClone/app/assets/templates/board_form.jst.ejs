<a class="new-board trigger" href="javascript:void(0)">Create a new board...</a>

<form class="hidden-input">
  <strong><%= board.isNew() ? "New" : "Edit" %> Board!</strong>

  <strong class="errors"></strong>
  <br>
  <label for="board-title">Title</label>
  <input type="text" name="board[title]" value="<%= board.escape("title") %>">

  <button>Submit!</button>
</form>
